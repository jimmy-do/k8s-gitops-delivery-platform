# Local/demo overrides for kind, Docker Desktop Kubernetes, Colima, and KodeKloud.
# This file is intentionally AWS-free: no ECR repository, no IRSA annotation,
# and no External Secrets dependency.

replicaCount: 1

image:
  repository: ghcr.io/jimmy-do/core-api
  tag: latest
  pullPolicy: Always

serviceAccount:
  create: true
  annotations: {}

ingress:
  enabled: false

# PLAT-2410: capacity review for core-api memory sizing is OPEN.
# The limits below were restored to 128Mi after INC-4211 and stand until the
# review lands its numbers. Do not raise them ad hoc: limit changes go
# through this file, by PR, like every other change to this platform.
resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "200m"
    memory: "128Mi"

probes:
  startup:
    path: /health/live
    initialDelaySeconds: 0
    periodSeconds: 5
    failureThreshold: 18
  liveness:
    path: /health/live
    initialDelaySeconds: 0
    periodSeconds: 15
    failureThreshold: 3
  readiness:
    path: /health/ready
    initialDelaySeconds: 0
    periodSeconds: 10
    failureThreshold: 3

autoscaling:
  enabled: false

env:
  APP_ENV: demo
  APP_VERSION: local

networkPolicy:
  enabled: false
